{
    "$id": "order.schema.json",
    "title": "POC order",
    "description": "Schema for POC order data",
    "type": "object",
    "properties": {
        "$schema": {
            "type": "string"
        },
        "orderDate": {
            "type": "string"
        },
        "sellerId": {
            "type": "string"
        },
        "buyerId": {
            "type": "string"
        },
        "productId": {
            "type": "string"
        },
        "price": {
            "type": "number"
        },
        "quantity": {
            "type": "number"
        },
        "requiredDate": {
            "type": "string"
        }
    },
    "required": [
        "orderDate",
        "sellerId",
        "buyerId",
        "productId",
        "currency",
        "quantity",
        "requiredDate"
    ],
    "additionalProperties": false
}